{
  "name": "Untitled GraphQL Schema",
  "schemaPath": "schema.graphql",
  "extensions": {
    "endpoints": {
      "Default GraphQL Endpoint": {
        "url": "https://api.github.com/graphql",
        "headers": {
          "user-agent": "JS GraphQL",
          "authorization": "Bearer ${env:REACT_APP_GITHUB_ACCESS_TOKEN}"
        },
        "introspect": false
      }
    }
  }
}